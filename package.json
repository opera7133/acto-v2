{
  "name": "acto-v2",
  "devDependencies": {
    "prettier": "^3.8.0"
  },
  "private": true,
  "scripts": {
    "dev": "bun run --parallel dev:blog dev:cms",
    "dev:blog": "bun run --filter blog dev",
    "build:blog": "bun run --filter blog build",
    "preview:blog": "bun run --filter blog preview",
    "dev:cms": "bun run --filter cms dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "astro-link-card": "^4.0.4"
  }
}
